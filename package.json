{
  "name": "tricycle-webapp",
  "version": "1.0.0",
  "scripts": {
    "client": "cd frontend && yarn start",
    "server": "cd backend && nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
  },
  "dependencies": {
    "express": "^4.16.2"
  },
  "devDependencies": {
    "concurrently": "^3.5.0"
  }
}
